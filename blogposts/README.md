# Blog Posts

Published and draft blog posts about porting models to the Apple Neural Engine.

Each post lives in its own subdirectory:

```
blogposts/
└── YYYY-MM-<slug>/
    ├── post.md          # The post itself (Markdown)
    └── assets/          # Images, GIFs, diagrams used in the post
```
